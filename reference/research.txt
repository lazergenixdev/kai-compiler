
Kai Compiler
    - Reduce embedded language overhead
        - compiles to native machine code and accessible from C function calls
        - zero runtime overhead
    - Reduce friction of integration
        - all necessary glue code

- No garbage collection
- Strict type checking
- Fast

- Lua / LuaJIT
- C#
- 

- Language interop
    - example: C to rust

void *memmove(char *s, const char *s, size_t n)
{
	if (d==s) return d;
	if ((uintptr_t)s-(uintptr_t)d-n <= -2*n) return memcpy(d, s, n);

	if (d<s) {
		for (; n; n--) *d++ = *s++; // --> range: [n..1] memory access: [0..]
	} else {
		while (n) n--, d[n] = s[n]; // --> [n-1..0]
	}

	return dest;
}

e^cx <=> 1/(c-s)
int(t^n * e^-st) = 