
// Basic Types
#export value1 : s32    = 0x45;  // 32-bit signed integer
#export value2 : f64    = 3.125; // 64-bit IEEE floating point
#export value3 : string = "wow"; // string type (pointer + length)


// Struct Types
Person :: struct {
    name   : string;
    age    : u32;
    height : u32;
}
#export you: Person; // defualt zero initialization


// Procedures
#export do_math :: (a: s32, b: s32, c: s32) -> s32
{
    ret (a + b) * (c - a - b);
}

